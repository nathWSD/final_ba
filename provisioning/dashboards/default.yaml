apiVersion: 1

providers:
  - name: 'ba-dashboards' 
    orgId: 1
    folder: ''          
    type: file
    disableDeletion: false
    editable: true
    options:
      # Path where Grafana will look for dashboards inside the container
      path: /var/lib/grafana/dashboards